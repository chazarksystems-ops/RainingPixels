# Rejected or Superseded Ideas

## Rejected for current direction

```text
flat tile-grid game as final direction
terminal-first playable target
menu-only colony sim
generic engine-first implementation
full ECS before substrate proof
LBM/full CFD for v0 water
infinite world before one chunk works
pretty tiles pretending to be material simulation
CUDA-only mainline runtime
```

## Superseded math claims

```text
double-buffering alone eliminates all races
all movement is mathematically race-free
simple min/max moisture formulas prove conservation
u32 packing eliminates bandwidth bottlenecks
peak bandwidth proves 60 FPS
taint ledger is complete by default
```

Use corrected math docs instead.

## Archive-only material

Old chunk0/macroquad/tile-grid docs are useful only as:
- historical prototype evidence,
- examples of what failed visually,
- backend behavior reference if needed.

They are not the current active game direction.
