# Failure Modes

This slice fails if:

- flags are defined inconsistently between packed and unpacked forms
- WGSL shifts do not match Rust shifts
- the aux byte is permanently named scent instead of remaining extensible
- scalar meaning is treated as universal physics instead of material-local interpretation
- gameplay logic sneaks into the cell-state contract
- tests are omitted
